@extends('layouts.app')
@section('title',$lesson['title'])
@section('content')<div class="container py-5"><div class="row g-5"><aside class="col-lg-3"><div class="toc"><a href="{{ route('materials') }}" class="small text-secondary"><i class="fa-solid fa-arrow-left me-1"></i> Semua materi</a><div class="card-clean p-3 mt-3"><div class="series-chip text-primary mb-2">{{ strtoupper($lesson['series']) }}</div><div class="fw-bold mb-3">Di halaman ini</div><nav class="nav flex-column small gap-2"><a class="text-secondary" href="#tujuan">Tujuan belajar</a>@foreach($lesson['topics'] as $i=>$topic)<a class="text-secondary" href="#topik-{{$i}}">{{ $topic['heading'] }}</a>@endforeach @if($lesson['codes'])<a class="text-secondary" href="#contoh">Contoh kode</a>@endif<a class="text-secondary" href="#latihan">Latihan</a></nav></div></div></aside><article class="col-lg-9 content-shell lesson-body"><span class="badge rounded-pill badge-soft mb-3">{{ $lesson['level'] }}</span><h1 class="display-5 display-title mb-3">{{ $lesson['title'] }}</h1><p class="lead text-secondary">Modul ringkas berbasis materi kuliah, diperbarui agar lebih relevan dengan HTML modern, Bootstrap 5, dan pola Laravel saat ini.</p><section id="tujuan" class="card-clean p-4 mt-4"><h2 class="mt-0 h5"><i class="fa-solid fa-bullseye text-primary me-2"></i>Tujuan Pembelajaran</h2><ul class="objective-list mb-0">@foreach($lesson['objectives'] as $o)<li>{{ $o }}</li>@endforeach</ul></section>
@foreach($lesson['topics'] as $i=>$topic)<section id="topik-{{$i}}"><h2>{{ $topic['heading'] }}</h2><p>{{ $topic['body'] }}</p></section>@endforeach
@if($lesson['codes'])<section id="contoh"><h2>Contoh Kode</h2>@foreach($lesson['codes'] as $code)<div class="code-card"><div class="code-head"><span>{{ strtoupper($code['language']) }}</span><button class="copy-code" data-copy><i class="fa-regular fa-copy"></i> Salin</button></div><pre><code>{{ $code['code'] }}</code></pre></div>@endforeach</section>@endif
<section id="latihan" class="mt-4"><div class="practice-box"><div class="eyebrow mb-2">Latihan</div><h2 class="mt-0 h5">Coba kerjakan</h2><p class="mb-0">{{ $lesson['practice'] }}</p></div></section><div class="d-flex justify-content-between gap-3 mt-5 pt-4 border-top">@if($previous)<a href="{{ route('lesson',$previous['slug']) }}" class="btn btn-outline-secondary"><i class="fa-solid fa-arrow-left me-2"></i><span class="d-none d-md-inline">{{ $previous['title'] }}</span><span class="d-md-none">Sebelumnya</span></a>@else<span></span>@endif @if($next)<a href="{{ route('lesson',$next['slug']) }}" class="btn btn-brand"><span class="d-none d-md-inline">{{ $next['title'] }}</span><span class="d-md-none">Berikutnya</span><i class="fa-solid fa-arrow-right ms-2"></i></a>@endif</div></article></div></div>@endsection